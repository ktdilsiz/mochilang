# MochiLang (web)

This is the MochiLang web app (Vite + React + TypeScript).

### Run locally

From the repo root:

```bash
pnpm -C apps/web dev
```

For the main project README, see `README.md` at the repo root.
