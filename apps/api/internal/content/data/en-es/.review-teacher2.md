# Teacher 2 Review — en-es B2, C1, C2

## Mechanical validation

```
/Users/kdilsiz/personal/code/mochilang/apps/api/internal/content/data/en-es/c1.json:gram2-3: dup pair rights
```

Only one mechanical issue across all three files: `c1.json` exercise `gram2-3` (Inversión enfática — Inicio/Estructura) has three pairs with `right: "+ aux + S"` (Never / Not only / Under no circumstances) and one with `"+ aux + S + V"` (Hardly … when). This is a pedagogically meaningful match-pairs (three triggers really do take the same structure), but it WILL break `match_pairs` rendering on the client because right-side duplicates collide. Easy fix: differentiate the right-side labels (e.g. add the trigger word, "+ aux + S (Never…)", or split into a different exercise type).

Everything else passes:
- No duplicate exercise IDs.
- All `multiple_choice` / `listen_and_choose` answers appear in their options.
- No empty `fill_blank` answers.
- All `tap_words_in_order` answers are reconstructible from their bank, and every tap_words exercise across all three files has distractors (no zero-distractor banks).
- All `tap_words` answers not embedded in their Spanish prompt.
- All dialogues have ≥ 2 speakers with unique IDs, valid turn kinds, valid speaker references, no unknown turn fields, and at least one interactive turn (B2: 11 dialogues avg 9.0 turns; C1: 9 dialogues avg 12.1 turns; C2: 9 dialogues avg 10.0 turns).
- Direction is consistent: Spanish in `prompt`/instruction text, English in `answer`/`spokenText`/`options`. (Spot-flagged proper nouns Almodóvar / García in Spanish-detection sweep, but those are intentional and the surrounding strings are English.)

## Overall verdict

**Ship it after one fix.** Content quality at all three levels is strong, pedagogically targeted, and linguistically accurate. Direction is correct. Idioms and false friends are explained accurately. C1 and C2 dialogues read like real conversations, not translation exercises. The one match_pairs bug is trivially fixable. There are a handful of minor quality nits below but none are blockers.

## b2.json
### Critical fixes
None.

### Quality concerns
- `r6-1.t3` dialogue fill: `before: "These things take time. Try not "` + answer `"to be"` + `after: " too hard on yourself, OK?"` accepts `"being"` as a length-mismatched alternative. Both reconstruct grammatical English ("try not to be too hard" / "try not being too hard"), but the gap rendering may surface "Try not being  too hard" with a double space. Cosmetic only.
- `c5-2` (modal perfect): `acceptableAnswers: ["must"]` for `"They should have arrived by now."` — "must" works ("they must have arrived") but shifts meaning from missed expectation to confident deduction. Could mislead a learner who is being taught the should/must contrast in the same lesson. Minor.
- `c4-7` (Wish & If only): accepts only `"only"` for "If ___ I had listened to her advice." Pedagogically clean, but the comma after "only" in the explanation would help — minor.
- `idi3-7` is in C1, not B2 — see C1 notes.
- B2 conditionals topic (`en-es-conditionals` 1–5) is genuinely B2: covers 2nd/3rd, mixed past→present, mixed present→past, `wish`/`if only`, modal perfect for regret and deduction. Mixed conditionals are explicitly drilled — a real Spanish-speaker pain point. Good.
- B2 phrasal-verbs (`en-es-relationships-1`), passive impersonal `It is said that…` with subject-raised version (`he is said to be…`), and BrE/AmE in education are all well-targeted Spanish-learner pain points.
- False friends lesson (`en-es-opinions-4`) is linguistically accurate: `actually`/`actualmente`, `sensible`/`sensato`, `support`/`apoyar`, `eventually`/`finalmente`. The `support` ≠ `soportar` distinction is explicitly stated.
- B2 hedging covers `sort of`, `kind of`, `I'm not sure I agree`, `with all due respect`, `I see your point, but…`. Appropriate B2 register.
- One small naturalness nit: `op2-9` "With all due respect, I don't quite agree with this idea" — "with this idea" is slightly stiff; native speakers would more likely say "with that" or just "I don't quite agree". Stylistic only.

## c1.json
### Critical fixes
1. `gram2-3` (en-es-c1-grammar-2 / Inversión enfática) — three pairs share `right: "+ aux + S"`. Will collide in match_pairs UI. **This is the only mechanical break in the data set.**

### Quality concerns
- C1 grammar topic genuinely targets C1: cleft sentences (it-cleft, wh-cleft, all-cleft, time-cleft), inversion (Never, Hardly…when, Not only, Under no circumstances, Only after), participle clauses (`Having considered…`), subjunctive `were` and `wish`. These are exactly the Spanish-learner C1 pain points. Strong.
- C1 idioms are correctly used in context (`bottom line`, `ballpark figure`, `move the needle`, `low-hanging fruit`, `throw someone under the bus`, `play devil's advocate`, `elephant in the room`, `barking up the wrong tree`, `sit on the fence`). One naturalness nit: `idi1-11` lists `"ride the elephant of doubt"` as a deliberate false idiom — that distractor is correctly flagged but it's so obviously fake that it's not great practice; a more plausibly-wrong distractor would be sharper.
- `idi3-7` claims `brush aside`, `brush off`, and `brush up` distinction lets "all valid" except `brush up` — the explanation correctly excludes `brush up`. However, the claim that `brush aside` and `brush off` are interchangeable elides a real nuance (`brush off` is more interpersonal/curt; `brush aside` is more about ideas/objections). Acceptable simplification at C1 — flag, not blocker.
- `gram-dlg.t3` (editorial rewrite): `before: "Never "` + answer `"before has"` + `after: " a single sector accounted..."` accepts `"has"` alone. Both reconstruct grammatical English (`Never before has...` / `Never has...`) but the latter has an awkward double-space and the lesson is explicitly about "Never before". Recommend removing `"has"` from acceptableAnswers.
- C1 business dialogue (`biz-dlg` / Q3 council) is genuinely C-level: the choice options force cleft usage, hedging, and acknowledge-and-pivot moves. Reads like a real board meeting transcript.
- C1 academic dialogue (`acad-dlg` / thesis defence) uses parallel-trends assumption, identification strategy, placebo tests — accurate methodological vocabulary. Distractors include the deliberately weak "It just holds, trust me" and "Honestly, I never thought about that" which are clearly wrong; the middle option is the correct register. Good.
- C1 hedging in academic context: `acad2-6` teaches `evidence` is uncountable (no "evidences"), `acad2-7` teaches `data` is plural in formal register — these are real C1 issues that intermediate Spanish speakers carry over from `evidencias` and `los datos son`. Excellent.
- `acad2-11` flags `sensible` false friend at C1 again — slight redundancy with B2 op4, but the academic register reinforcement is fine.

## c2.json
### Critical fixes
None.

### Quality concerns
- C2 register switching: `en-es-c2-irony-1` (understatement británico) is well-pitched. "not bad" → "muy bien", "with the greatest respect" → "voy a contradecirte", "we have one or two minor concerns" → varias preocupaciones serias. These are exactly the C2 sociopragmatic competence items.
- C2 Latinate vs Germanic (`en-es-c2-literary-1`): `commence/begin`, `terminate/end`, `assist/help`, `perceive/see`, `discern`, `evoke`, `fragrance`, `concluded the night in a twilight-like silence`. Register is correctly literary; vocabulary is accurate.
- `lit1-9` "She concluded the night in a twilight-like silence" — the answer is grammatical but slightly purple/odd English. "Twilight-like" is rare; "twilit" would be more natural literary English. Stylistic.
- C2 modal-perfect philosophical use (`ph1-*`): `could have been otherwise` (contrafactual modal claim — actual philosophical use), `must have known`, `ought to have been`. `ph1-8` explicitly teaches that `"could of"` is an error and should be `"could have"` — a real C2 sophistication point.
- C2 idioms: `a curate's egg`, `damning with faint praise`, `a Pyrrhic victory`, `the elephant in the room`, `to throw in the towel`, `to bury the hatchet`, `take the wind out of someone's sails`. All correctly used in context.
- C2 regional (BrE/AmE) — `lift`/`elevator`, `boot`/`trunk`, `biscuit`/`cookie`, `lorry`/`truck`, `crisps`, `queue`, "pants = ropa interior en UK". All accurate. The "holiday" vs "vacation" distinction is included.
- C2 poetry/allusions: Shakespeare (`to be or not to be`, `star-crossed lovers`, `out, damned spot`, `the lady doth protest too much`, `pound of flesh`, `wherefore art thou`, `tale told by an idiot`, `all the world's a stage`) — all canonical and correctly sourced. Biblical/classical (`writing on the wall — Daniel`, `Pandora's box`, `Achilles' heel`, `crossing the Rubicon`, `feet of clay`, `good Samaritan`, `prodigal son`, `thorn in my side`) — all sourced correctly.
- C2 dialogues: literary book club (`lit5-1`), satirical writers' room (`ir4-1`), poetry Q&A. The satirical-room dialogue specifically tests technical understanding of irony (`'reassuring' is doing too much work — flatter is funnier`) — genuinely C2-level metalinguistic awareness.
- C2 rhetoric: `anaphora`, `epistrophe`, `chiasmus`, `antithesis`, `tricolon`, with canonical examples (Kennedy chiasmus, Lincoln epistrophe, Churchill anaphora, Dickens antithesis). Correct.
- One minor C2 nit: `ir1-3` answer `"having a bit"` for "We're ___ of a problem here" with `before: "We're "` and `after: " of a problem here"` reconstructs as "We're having a bit of a problem here" — grammatical and natural. Good.
- Spelling consistency: `gloomy`/`gloom` distractor pair at `lit1-6` is good. `odour`/`odor` both accepted at `lit1-10` — BrE/AmE awareness. Good.

## Cross-cutting observations

- **Direction is correct everywhere.** Spanish in instructions/prompts/Spanish-side of match pairs, English everywhere it should be. Where instruction text contains an English phrase being explained, that's expected and correct.
- **Pacing across CEFR is well-calibrated.** B2 hits mixed conditionals, modal perfect, passive impersonal/causative, false friends, register-aware hedging. C1 escalates to cleft, inversion, participle clauses, subjunctive `were`, academic hedging, sophisticated idioms. C2 covers Latinate/Germanic register, understatement, classical/biblical allusion, philosophical modal nuance, regional variation. No level reads like a recycled lower level with bigger vocabulary.
- **Spanish-learner-specific pain points are addressed:**
  - Mixed conditionals (B2): explicit, with both directions (past→present and present→past).
  - `actually`/`actualmente` (B2 + reinforced C1): explicit false-friend drill.
  - `support` ≠ `soportar` (B2 + C1): explicit, with `put up with` / `stand` / `tolerate` taught as the real equivalent.
  - `sensible` (B2 + C1): explicit.
  - `evidence` uncountable, `data` plural in formal register (C1): explicit.
  - Phrasal verbs throughout (B2 relationships, C1 phrasal-verbs lesson, C2 nuanced phrasal lesson).
  - Cleft + inversion (C1): explicit topic with sub-lessons.
  - Latinate/Germanic register (C2): explicit topic.
  - "could of" → "could have" (C2): explicit.
  - Understatement / register switching (C2): explicit topic.
- **Dialogue quality is the strongest asset.** C1 thesis-defence, Q3 council, op-ed editor, conference panel, and C2 literary book-club, satirical writers' room, philosophy seminar all read like real professional/academic English. Distractor wrong-answers are well-engineered: one is grammatically wrong, one is register-wrong, one is content-wrong. Pedagogically tight.
- **Type-balance across levels is uniform and sensible:** roughly mc 30–35%, fb 25–30%, tap 15–20%, lc 7%, mp 7%, dialogue 1–2% (one per topic). Match-pairs is used pedagogically (for paradigms and idiom-meaning mapping) rather than as filler.
- **Total content:** B2 ~561 ex / 66 lessons / 11 topics; C1 ~405 ex / 45 lessons / 9 topics; C2 ~299 ex / 38 lessons / 9 topics. Density is right for the level — C2 has fewer but heavier items, which is appropriate.
- **One global suggestion (non-blocking):** the `level` and `courseId` fields appear missing from the top-level JSON shape in all three files (or they exist but were not surfaced in my sampling). If the data layer expects them, double-check; if not, ignore.
